# git Commands
#global config
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
git config --global --list

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/JagdishChavanWork/Project1.git
git push -u origin main


pull code from repo
git pull